<style>
    .outer-div {
        display: flex; 
        justify-content: center;
        height: 100%;
        font-family: Arial, Helvetica, sans-serif;
    }
    .inner-div {
        display: flex;
        justify-content: center;
        flex-direction: column;
    }
    .form {
        display: flex;
        flex-direction: column;
    }

    .form-outer-div {
        background-color: rgb(203, 203, 203);
        padding: 24px;
        border-radius: 12px;
    }
    input {
        padding: 12px;
        font-size: 14px;
        margin-bottom: 12px;
        border-radius: 6px;
        border: solid 1px rgb(71, 71, 79);
    }
    button {
        background-color: rgb(246, 206, 86);
        border-radius: 6px;
        font-size: 14px;
        font-weight: bold;
        color: rgb(71, 71, 79);
        padding: 12px;
        border-radius: 6px;
        border: solid 1px rgb(71, 71, 79);
    }
    .login-text {
        font-weight: bold;
        font-size: 16px;
        margin-bottom: 16px;
        text-align: center;
    }
    img {
        border-radius: 12px;
    }
    .outbreak {
        font-size: 50px;
        color: rgb(246, 206, 86);
        font-weight: bold;
        text-align: center;
        padding: 12px;
    }
    a {
        color: rgb(71, 71, 79);  
    }
</style>

<script>

    async function handleLogin() {
        console.log("Login...")
        
        const email = document.getElementById('email').value
        const password = document.getElementById('password').value

        const formData = new FormData()
        formData.append("emailAddress", email)
        formData.append("password", password)

        try {
            const response = await axios.put("/api/v1/entrance/login", formData)
            document.location = "/post"
        } catch (error) {
            console.error(error.toString())
        }

        
    }

</script>

<div class="outer-div">

    <div class="inner-div">

       
        <img src="/images/virus.png" alt="">
        <div class="outbreak">
            OUTBREAK
        </div>
    

        <div class="form-outer-div">

            <div class="login-text">
                Log into your account
            </div>

            <form class="form" action="/api/v1/entrance/login" method="POST">
                <input id="email" name="emailAddress" type="text" placeholder="Email">
                <input id="password" name="password" type="password" placeholder="Password">
                <button onclick="handleLogin()" type="button">Login</button>
            </form>

            <div>
                Don't have an account?
                <a  href="/signup">Sign up</a>.
            </div>

        </div>
        
    </div>

    
</div>